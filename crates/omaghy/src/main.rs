//! omaghy — a full GitHub client for the terminal.
//!
//! Modes: `tui` (default), `watch`, `doctor`. See `spec/00-overview.md`.

fn main() {
    println!("omaghy: not implemented yet — see spec/");
}
